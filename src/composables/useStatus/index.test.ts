import {
  describe,
  expect,
  it
} from '@jest/globals'

import {
  useStatus
} from './'

describe('useStatus', () => {
  it('Should Be Defined', () => {
    expect(useStatus).toBeDefined()
  })
})