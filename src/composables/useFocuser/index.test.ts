import {
  describe,
  expect,
  it
} from '@jest/globals'

import {
  useFocuser
} from './'

describe('useFocuser', () => {
  it('Should Be Defined', () => {
    expect(useFocuser).toBeDefined()
  })
})